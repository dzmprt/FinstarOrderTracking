export interface Notification {
  id: string;
  message: string;
  timestamp: string;
}

export interface StatusOption {
  value: number;
  label: string;
}

export interface SortOption {
  value: string;
  label: string;
}
